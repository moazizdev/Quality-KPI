# DATABASE_URL=postgresql://user:password@localhost:5432/quality_kpi
# For SQLite (development):
DATABASE_URL=sqlite:///./quality_kpi.db
APP_ENV=production
# JWT secret key (change in production)
SECRET_KEY=change-me-in-production
